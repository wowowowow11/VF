# =========================================================================
# VF6.PY UDS 诊断自动化测试项目 - 依赖清单
# 生成方式: pip freeze > requirements.txt 基础上精简，保留项目直接依赖
# 安装命令: pip install -r requirements.txt
# =========================================================================

# ---------------------------- 核心依赖 ----------------------------
# YAML 测试用例解析
PyYAML==6.0.3

# ------------------------- 测试框架依赖 -------------------------
# 测试框架核心
pytest==8.3.5

# Allure 测试报告生成 (pytest 插件 + 公共库)
allure-pytest==2.16.0
allure-python-commons==2.16.0

# pytest 内部依赖 (由 pytest 自动安装，此处显式锁定版本以保证一致性)
iniconfig==2.1.0
packaging==26.2
pluggy==1.5.0
tomli==2.4.1
exceptiongroup==1.3.1

# Allure 内部依赖
attrs==25.3.0
